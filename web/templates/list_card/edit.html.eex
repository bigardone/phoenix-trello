<h2>Edit list card</h2>

<%= render "form.html", changeset: @changeset,
                        action: list_card_path(@conn, :update, @list_card) %>

<%= link "Back", to: list_card_path(@conn, :index) %>
