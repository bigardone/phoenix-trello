<h2>Edit board card</h2>

<%= render "form.html", changeset: @changeset,
                        action: board_card_path(@conn, :update, @board_card) %>

<%= link "Back", to: board_card_path(@conn, :index) %>
